import { TestBed } from '@angular/core/testing';

import { SnHeaderComponent } from './header.component';

describe('App', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [SnHeaderComponent]
    })
  })

  it ('should work', () => {
    expect(true).toBe(true);
  })
})
